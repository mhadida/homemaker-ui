# Optional: model used by the AI facade prompt route.
HOMEMAKER_AI_MODEL=openai/gpt-5-nano

# Required for local AI Gateway requests. The editor's local keyword parser
# remains usable when this is absent.
AI_GATEWAY_API_KEY=
